Optional isPresent vs empty()
Functional Inteface with Runnable


default methods from interface can be overriden
but
static methods of an interface can't be overriden.

---

Having default methods also gives flexibility to call
default functions from the class which implements the interface with default function

